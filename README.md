# Конфигурационный парсер (Вариант 18)

Инструмент командной строки для преобразования учебного конфигурационного языка в JSON.

## 📋 Требования задания

**Вариант №18:** Разработать инструмент командной строки для учебного конфигурационного языка, который преобразует текст из входного формата в JSON.

### Спецификация входного языка:

1. **Вход:** стандартный ввод (stdin)
2. **Выход:** стандартный вывод (stdout), формат JSON
3. **Комментарии:**
   - Однострочные: `\ Это комментарий`
   - Многострочные:
     ```text
     (comment
     Это многострочный
     комментарий
     )
     ```
4. **Числа:** `[+-]?\d+\.?\d*[eE][+-]?\d+` (научная нотация, например: `1.2e+3`, `-5.0E-2`)
5. **Словари:**
   ```text
   {
       имя = значение;
       имя = значение;
   }
